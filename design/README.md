# Design Runtime

The design skills look for an optional AI mockup binary at:

```text
$HOME/.gstack-lite/design/dist/design
```

This repository does not require that binary for the default install. If it is missing or no API key is available, design workflows should continue with written design direction, wireframes, available screenshots, and `design-html/vendor/pretext.js`.
